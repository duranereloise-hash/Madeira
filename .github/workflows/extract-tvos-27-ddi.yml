name: Extract tvOS 27 DDI via setup-xcode

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  extract-ddi:
    name: Extract tvOS 27 DDI
    runs-on: xcode-27
    timeout-minutes: 40

    steps:
      - name: Xcode version
        run: xcodebuild -version

      - name: Find tvOS 27 DDI
        run: |
          echo "=== tvOS_DDI version ==="
          cat /Library/Developer/DeveloperDiskImages/tvOS_DDI/version.plist
          echo "=== ProductBuildVersion ==="
          grep 'ProductBuildVersion' /Library/Developer/DeveloperDiskImages/tvOS_DDI/Restore/BuildManifest.plist
          echo "=== Restore contents ==="
          find /Library/Developer/DeveloperDiskImages/tvOS_DDI -type f

      - name: Package and Upload
        run: |
          mkdir -p tvos27-ddi
          cp -R /Library/Developer/DeveloperDiskImages/tvOS_DDI/ tvos27-ddi/
          du -sh tvos27-ddi

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: tvOS27-DDI
          path: tvos27-ddi/
          if-no-files-found: error